<!DOCTYPE html>
<html>
  <head>
    <title>Meetup</title>
  </head>
  <body>
    <% if !@message.nil? && @message != '' %>
      <%= @message %>
    <% end %>
    <p>Meetup Name: <%= @meetup.first.name %></p>
    <p>Description: <%= @meetup.first.description %></p>
    <p>Location: <%= @meetup.first.location %></p>
    <% if !@members.nil? %>
    <p>
      <ul>
        <% @members.each_with_index do |mem, i| %>
          <li><%= @members[i].username %></li>
        <% end %>
      </ul>
    </p>
    <% end %>
    <p>Creator: <%= @creator.first.username %></p>
    <form action="/meetups/<%= @meetup.first.id %>" method='get'>
    	<input type="hidden" name="join" id='join' value='1'>
    	<button type='submit' style="background-color:black">Join Meetup</button>
    </form>


  </body>
</html>